"""Deployment package — reads all settings from the central ``config.py``.

Nothing in here hard-codes a repository, PythonAnywhere username, domain,
path or endpoint. Change ``config.py`` and the whole deploy pipeline
follows.
"""
